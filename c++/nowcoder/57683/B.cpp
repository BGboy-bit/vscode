#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef double db;

ll n, x, y;

void Solve() {
    cin >> n;
    cout << n << " " << n * 2 << "\n"; 
}


int main() {
    ios::sync_with_stdio(false);
    cin.tie(0); cout.tie(0);
    ll T = 1;
    cin >> T;
    while(T --) {
        Solve();
    }
    return 0;
}